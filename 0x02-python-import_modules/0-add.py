#!/usr/bin/python3
if __name__ == "__main__":
    # import the module add_0
    from add_0 import add

    # variable declaration and assignment
    a = 1
    b = 2
    print("{} + {} = {}".format(a, b, add(a, b)))
